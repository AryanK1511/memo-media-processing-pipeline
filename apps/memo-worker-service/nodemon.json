{
	"watch": ["src"],
	"ext": "ts,json",
	"ignore": ["src/**/*.spec.ts"],
	"exec": "ts-node -r tsconfig-paths/register --project tsconfig.app.json src/main.ts",
	"env": {
		"NODE_ENV": "development"
	},
	"delay": "1000",
	"verbose": true
}
